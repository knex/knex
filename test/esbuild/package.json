{
  "name": "esbuild",
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.14.27"
  },
  "dependencies": {
    "@vscode/sqlite3": "^5.0.7",
    "knex": "^1.0.4",
    "sqlite3": "^5.0.2"
  },
  "scripts": {
    "esbuild": "esbuild console.js --outfile=console.bundle.cjs --bundle --platform=node"
  }
}
